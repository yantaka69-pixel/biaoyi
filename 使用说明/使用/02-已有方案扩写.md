# 已有方案扩写

在左侧点击 **标书生成 → 已有方案扩写**。

第一步需要同时导入招标文件和已经写好的技术方案。随后选择目录处理方式：

- **仅使用原方案目录**：保持原目录结构。
- **AI 补充目录**：在原目录基础上补充招标文件要求但原方案缺少的内容。

如需调整旧目录提取方式，可先到 **设置 → 智能体配置** 修改“已有方案扩写-旧目录提取”并保存。

后续仍按“文件解析 → 目录 → 全局事实 → 正文 → 导出 Word”的顺序操作。生成目录前也可以设置目标字数和每小节字数。软件会保留原方案已有正文，并为缺失或需要扩充的部分生成内容。
